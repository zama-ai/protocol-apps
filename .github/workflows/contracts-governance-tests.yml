# Run tests
name: contracts-governance-tests

on:
  pull_request:

  push:
    branches:
      - main
  workflow_dispatch:

permissions: {}

concurrency:
  group: ci-contracts-governance-tests-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  check-changes:
    name: contracts-governance-tests/check-changes
    permissions:
      actions: 'read' # Required to read workflow run information
      contents: 'read' # Required to checkout repository code
      pull-requests: 'read' # Required to read pull request information
    runs-on: ubuntu-latest
    outputs:
      changes-contracts-governance: ${{ steps.filter.outputs.contracts-governance }}
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: 'false'
      - uses: dorny/paths-filter@de90cc6fb38fc0963ad72b210f1f284cd68cea36  # v3.0.2
        id: filter
        with:
          filters: |
            contracts-governance:
              - .github/workflows/contracts-governance-tests.yml
              - contracts/governance/**
  tests:
    name: contracts-governance-tests/tests (bpr)
    needs: check-changes
    if: ${{ needs.check-changes.outputs.changes-contracts-governance == 'true' }}
    runs-on: ubuntu-latest
    permissions:
      contents: 'read' # Required to checkout repository code
    steps:
      - name: Checkout project
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: 'false'

      - name: Install pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5.0.0
        with:
          version: 9

      - name: Setup Node
        uses: actions/setup-node@60edb5dd545a775178f52524783378180af0d1f8 # v4.0.2
        with:
          node-version: 20.x
          cache: pnpm
          cache-dependency-path: contracts/governance/pnpm-lock.yaml

      - name: Install dependencies
        working-directory: contracts/governance
        run: pnpm install --frozen-lockfile

      - name: Run compile
        working-directory: contracts/governance
        run: pnpm run compile

      - name: Run unit tests
        working-directory: contracts/governance
        run: pnpm test
