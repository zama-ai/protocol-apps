# Run tests
name: contracts-confidential-token-wrappers-registry-tests
on:
  pull_request:
    paths:
      - '.github/workflows/contracts-confidential-token-wrappers-registry-tests.yml'
      - 'contracts/confidential-token-wrappers-registry/**'
  push:
    branches:
      - main
  workflow_dispatch:
permissions: {}
concurrency:
  group: ci-contracts-confidential-token-wrappers-registry-tests-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}
jobs:
  tests:
    name: contracts-confidential-token-wrappers-registry-tests/tests (bpr)
    runs-on: ubuntu-latest
    permissions:
      contents: 'read' # Required to checkout repository code
    steps:
      - name: Workaround CVE-2026-31431 (copy.fail)
        run: echo "install algif_aead /bin/false" | sudo tee /etc/modprobe.d/disable-algif-aead.conf; if lsmod | grep -q algif_aead; then sudo rmmod algif_aead; fi
      - name: Checkout project
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: 'false'
      - name: Install dependencies
        working-directory: contracts/confidential-token-wrappers-registry
        run: npm ci
      - name: Run conformance
        working-directory: contracts/confidential-token-wrappers-registry
        run: make conformance
      - name: Run compile
        working-directory: contracts/confidential-token-wrappers-registry
        run: make compile
      - name: Run unit tests
        working-directory: contracts/confidential-token-wrappers-registry
        run: make test
