# Run tests
name: contracts-fees-burner-tests

on:
  pull_request:
    paths:
      - '.github/workflows/contracts-fees-burner-tests.yml'
      - 'contracts/feesBurner/**'
  push:
    branches:
      - main
  workflow_dispatch:

permissions: {}

concurrency:
  group: ci-contracts-fees-burner-tests-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  tests:
    name: contracts-fees-burner-tests/tests (bpr)
    runs-on: ubuntu-latest
    permissions:
      contents: 'read' # Required to checkout repository code
    steps:
      - name: Checkout project
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: 'false'

      - name: Install dependencies
        working-directory: contracts/feesBurner
        run: npm ci

      - name: Run unit tests
        working-directory: contracts/feesBurner
        run: npx hardhat test
